# 开发环境Dockerfile
FROM node:18-alpine

WORKDIR /app

# 安装开发依赖
RUN npm install -g @vue/cli-service

# 复制package文件
COPY package*.json ./

# 安装依赖
RUN npm install

# 暴露端口
EXPOSE 5173

# 启动开发服务器
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
